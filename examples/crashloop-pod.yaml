apiVersion: v1
kind: Pod
metadata:
  name: crashloop-app
  namespace: kube-events-demo
  labels:
    app: crashloop-app
spec:
  restartPolicy: Always
  containers:
    - name: failing
      image: busybox:1.37
      command: ["sh", "-c", "echo 'starting...'; sleep 2; exit 1"]
